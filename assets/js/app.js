/* ============================================
   app.js
   Ponto de entrada da aplicação.
   Inicializa todos os módulos quando o DOM
   estiver pronto.
   ============================================ */

document.addEventListener('DOMContentLoaded', () => {
    Navigation.init();
    Animations.init();
});
